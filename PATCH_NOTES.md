# Patch 0021 — Flet Audio 0.86.5 compatibility

- Исправлена несовместимая зависимость `flet-audio==0.1.0`, которая импортировала удалённый модуль `flet.core`.
- `flet-audio` закреплён на `0.86.5`, в точности как основная зависимость `flet==0.86.5`.
- Waveform/player код не менялся: используется API Audio extension ветки Flet 0.86.5.
- Regression-тест проверяет правильный pin и запрещает возврат к `0.1.0`.
